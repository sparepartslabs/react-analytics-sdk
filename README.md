# Spare Parts React Analytics SDK

Modular React and TypeScript analytics for Spare Parts. Applications opt into only the modules they need; the first module provides private in-app feedback that creates issues in a workspace intake repository through Spare Parts Core.

The package will be published as `@sparepartslabs/react-analytics-sdk`.
